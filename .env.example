# Cohere API keys — copy to .env and fill in. Never commit .env.
# Production key: used by the offline pack build pipeline (corpus embedding).
COHERE_API_KEY_PRODUCTION=
# Trial key: used for local dev/testing of the runtime query path.
COHERE_API_KEY_TRIAL=
