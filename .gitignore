# Secrets
.env
cohere_keys.txt

# Dependencies / build output (populated in later phases)
node_modules/
dist/
target/

# Python pipeline
pipeline/.venv/
pipeline/.cache/
__pycache__/
*.pyc

# Eval harness (scripts + golden sets are committed; outputs are not)
eval/.venv/
eval/out/

# Built pack artifacts (shipped via installer, not the repo)
packs-out/

# OS noise
Thumbs.db
Desktop.ini
assets/demo.gif
